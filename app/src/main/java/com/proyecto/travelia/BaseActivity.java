package com.proyecto.travelia;

// BaseActivity was added but now removed to restore original project state.
// If you want to reintroduce a base activity, recreate it with the desired shared logic.

public class BaseActivity {
    // placeholder to avoid missing class errors during transitional edits
}